# Tema 8: Documentación, Testing y Monitorización

## Objetivos

- Documentar APIs con Swagger (OpenAPI 3.0)
- Implementar tests automatizados con Jest y Supertest
- Configurar monitorización de errores con Slack

---

## Índice

1. [Documentación con Swagger](#1-documentación-con-swagger)
2. [Testing con Jest](#2-testing-con-jest)
3. [Monitorización con Slack](#3-monitorización-con-slack)
4. [Ejercicio del Curso](#4-ejercicio-del-curso)
5. [Resumen](#5-resumen)
6. [Estructura Final del Proyecto](#6-estructura-final-del-proyecto)

---

## 1. Documentación con Swagger

> 📂 **Código de ejemplo**: [`codigo/T8/src/docs/swagger.js`](../codigo/T8/src/docs/swagger.js)

### 1.1 ¿Qué es Swagger?

**Swagger** es un conjunto de herramientas de código abierto para diseñar, construir y documentar APIs RESTful. Sigue la especificación **OpenAPI 3.0**.

Beneficios:
- Documentación interactiva auto-generada
- Los clientes pueden probar la API desde el navegador
- Contrato claro entre frontend y backend

### 1.2 Instalación

```bash
npm install swagger-ui-express swagger-jsdoc
```

### 1.3 Configuración de Swagger

```javascript
// src/docs/swagger.js
import swaggerJsdoc from 'swagger-jsdoc';

const options = {
  definition: {
    openapi: '3.0.3',
    info: {
      title: 'API de Tracks - Express con Swagger',
      version: '1.0.0',
      description: 'API REST documentada con Swagger',
      license: {
        name: 'MIT',
        url: 'https://spdx.org/licenses/MIT.html'
      },
      contact: {
        name: 'Tu Nombre',
        email: 'tu@email.com'
      }
    },
    servers: [
      {
        url: 'http://localhost:3000',
        description: 'Servidor de desarrollo'
      }
    ],
    components: {
      securitySchemes: {
        bearerAuth: {
          type: 'http',
          scheme: 'bearer',
          bearerFormat: 'JWT'
        }
      },
      schemas: {
        User: {
          type: 'object',
          required: ['name', 'email', 'password'],
          properties: {
            name: { type: 'string', example: 'Juan Pérez' },
            email: { type: 'string', format: 'email', example: 'juan@ejemplo.com' },
            password: { type: 'string', format: 'password', example: 'MiPassword123' },
            age: { type: 'integer', example: 25 },
            role: { type: 'string', enum: ['user', 'admin'], default: 'user' }
          }
        },
        Track: {
          type: 'object',
          required: ['title', 'artist', 'duration'],
          properties: {
            title: { type: 'string', example: 'Mi Canción' },
            artist: { type: 'string', example: '65f8b3a2c9d1e20012345678' },
            duration: { type: 'integer', example: 180 },
            genres: { 
              type: 'array', 
              items: { type: 'string' },
              example: ['rock', 'pop']
            }
          }
        },
        Login: {
          type: 'object',
          required: ['email', 'password'],
          properties: {
            email: { type: 'string', format: 'email' },
            password: { type: 'string', format: 'password' }
          }
        },
        Error: {
          type: 'object',
          properties: {
            error: { type: 'boolean', example: true },
            message: { type: 'string', example: 'Error message' }
          }
        }
      }
    }
  },
  apis: ['./src/routes/*.js']
};

export default swaggerJsdoc(options);
```

### 1.4 Integración en app.js

```javascript
import swaggerUi from 'swagger-ui-express';
import swaggerSpecs from './docs/swagger.js';

// Después de los middlewares, antes de las rutas
app.use('/api-docs', swaggerUi.serve, swaggerUi.setup(swaggerSpecs));
```

Accede a la documentación en: `http://localhost:3000/api-docs`

### 1.5 Documentar Rutas con JSDoc

```javascript
// src/routes/auth.routes.js

/**
 * @openapi
 * /api/auth/register:
 *   post:
 *     tags:
 *       - Auth
 *     summary: Registrar nuevo usuario
 *     description: Crea una cuenta de usuario y devuelve un token JWT
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             $ref: '#/components/schemas/User'
 *     responses:
 *       201:
 *         description: Usuario creado exitosamente
 *         content:
 *           application/json:
 *             schema:
 *               type: object
 *               properties:
 *                 token:
 *                   type: string
 *                 user:
 *                   $ref: '#/components/schemas/User'
 *       409:
 *         description: El email ya existe
 *         content:
 *           application/json:
 *             schema:
 *               $ref: '#/components/schemas/Error'
 */
router.post('/register', validate(registerSchema), registerCtrl);

/**
 * @openapi
 * /api/auth/login:
 *   post:
 *     tags:
 *       - Auth
 *     summary: Iniciar sesión
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             $ref: '#/components/schemas/Login'
 *     responses:
 *       200:
 *         description: Login exitoso
 *       401:
 *         description: Credenciales inválidas
 */
router.post('/login', validate(loginSchema), loginCtrl);
```

### 1.6 Documentar Rutas Protegidas

```javascript
/**
 * @openapi
 * /api/tracks:
 *   post:
 *     tags:
 *       - Tracks
 *     summary: Crear un track
 *     security:
 *       - bearerAuth: []
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             $ref: '#/components/schemas/Track'
 *     responses:
 *       201:
 *         description: Track creado
 *       401:
 *         description: No autorizado
 *       403:
 *         description: Sin permisos
 */
router.post('/', authMiddleware, checkRol(['user', 'admin']), createTrack);
```

### 1.7 Documentar Parámetros de Ruta

```javascript
/**
 * @openapi
 * /api/tracks/{id}:
 *   get:
 *     tags:
 *       - Tracks
 *     summary: Obtener track por ID
 *     parameters:
 *       - name: id
 *         in: path
 *         required: true
 *         description: ID del track (MongoDB ObjectId)
 *         schema:
 *           type: string
 *     responses:
 *       200:
 *         description: Track encontrado
 *       404:
 *         description: Track no encontrado
 */
router.get('/:id', getTrack);
```

---

## 2. Testing con Jest

### 2.1 ¿Por qué Jest?

**Jest** es el framework de testing más popular para JavaScript:
- Zero config para empezar
- Mocking integrado
- Coverage de código
- Watch mode para desarrollo

**Supertest** permite hacer peticiones HTTP a tu API en los tests.

### 2.2 Instalación

```bash
npm install --save-dev jest supertest
```

### 2.3 Configuración en package.json

```json
{
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --forceExit --detectOpenHandles",
    "test:watch": "npm test -- --watch",
    "test:coverage": "npm test -- --coverage"
  }
}
```

> **Nota**: `--experimental-vm-modules` es necesario para ESM.

### 2.4 Exportar la App

```javascript
// src/app.js
// Al final del archivo, exportar para tests
export default app;
```

### 2.5 Primer Test

```javascript
// tests/auth.test.js
import request from 'supertest';
import app from '../src/app.js';

describe('Auth Endpoints', () => {
  let token = '';
  let userId = '';
  
  // Usuario de prueba único
  const testUser = {
    name: 'Test User',
    email: `test_${Date.now()}@example.com`,
    password: 'TestPassword123'
  };

  describe('POST /api/auth/register', () => {
    it('debería registrar un nuevo usuario', async () => {
      const res = await request(app)
        .post('/api/auth/register')
        .send(testUser)
        .expect('Content-Type', /json/)
        .expect(201);
      
      expect(res.body).toHaveProperty('token');
      expect(res.body).toHaveProperty('user');
      expect(res.body.user.email).toBe(testUser.email);
      expect(res.body.user.role).toBe('user');
      expect(res.body.user).not.toHaveProperty('password');
      
      token = res.body.token;
      userId = res.body.user._id;
    });

    it('debería rechazar email duplicado', async () => {
      const res = await request(app)
        .post('/api/auth/register')
        .send(testUser)
        .expect(409);
      
      expect(res.body.error).toBe(true);
    });

    it('debería rechazar datos inválidos', async () => {
      const res = await request(app)
        .post('/api/auth/register')
        .send({ email: 'invalid' })
        .expect(400);
      
      expect(res.body.error).toBe(true);
    });
  });

  describe('POST /api/auth/login', () => {
    it('debería hacer login correctamente', async () => {
      const res = await request(app)
        .post('/api/auth/login')
        .send({
          email: testUser.email,
          password: testUser.password
        })
        .expect(200);
      
      expect(res.body).toHaveProperty('token');
      token = res.body.token;
    });

    it('debería rechazar password incorrecto', async () => {
      await request(app)
        .post('/api/auth/login')
        .send({
          email: testUser.email,
          password: 'WrongPassword123'
        })
        .expect(401);
    });

    it('debería rechazar usuario inexistente', async () => {
      await request(app)
        .post('/api/auth/login')
        .send({
          email: 'noexiste@example.com',
          password: 'TestPassword123'
        })
        .expect(404);
    });
  });

  describe('Rutas protegidas', () => {
    it('debería acceder con token válido', async () => {
      const res = await request(app)
        .get('/api/auth/me')
        .set('Authorization', `Bearer ${token}`)
        .expect(200);
      
      expect(res.body.email).toBe(testUser.email);
    });

    it('debería rechazar sin token', async () => {
      await request(app)
        .get('/api/auth/me')
        .expect(401);
    });

    it('debería rechazar token inválido', async () => {
      await request(app)
        .get('/api/auth/me')
        .set('Authorization', 'Bearer token_invalido')
        .expect(401);
    });
  });

  // Limpiar después de los tests
  afterAll(async () => {
    if (userId) {
      await request(app)
        .delete(`/api/users/${userId}`)
        .set('Authorization', `Bearer ${token}`);
    }
  });
});
```

### 2.6 Test de Tracks

```javascript
// tests/tracks.test.js
import request from 'supertest';
import app from '../src/app.js';

describe('Tracks Endpoints', () => {
  let token = '';
  let trackId = '';
  
  // Crear usuario para obtener token
  beforeAll(async () => {
    const user = {
      name: 'Track Tester',
      email: `tracks_${Date.now()}@example.com`,
      password: 'TestPassword123'
    };
    
    const res = await request(app)
      .post('/api/auth/register')
      .send(user);
    
    token = res.body.token;
  });

  describe('GET /api/tracks', () => {
    it('debería obtener lista de tracks', async () => {
      const res = await request(app)
        .get('/api/tracks')
        .expect(200);
      
      expect(Array.isArray(res.body.data)).toBe(true);
    });
  });

  describe('POST /api/tracks', () => {
    it('debería crear un track', async () => {
      const track = {
        title: 'Test Track',
        duration: 180,
        genres: ['rock']
      };
      
      const res = await request(app)
        .post('/api/tracks')
        .set('Authorization', `Bearer ${token}`)
        .send(track)
        .expect(201);
      
      expect(res.body.data.title).toBe(track.title);
      trackId = res.body.data._id;
    });

    it('debería rechazar sin autenticación', async () => {
      await request(app)
        .post('/api/tracks')
        .send({ title: 'Sin Auth' })
        .expect(401);
    });
  });

  describe('GET /api/tracks/:id', () => {
    it('debería obtener un track por ID', async () => {
      const res = await request(app)
        .get(`/api/tracks/${trackId}`)
        .expect(200);
      
      expect(res.body.data._id).toBe(trackId);
    });

    it('debería devolver 404 para ID inexistente', async () => {
      await request(app)
        .get('/api/tracks/65f8b3a2c9d1e20012345678')
        .expect(404);
    });
  });

  describe('PUT /api/tracks/:id', () => {
    it('debería actualizar un track', async () => {
      const res = await request(app)
        .put(`/api/tracks/${trackId}`)
        .set('Authorization', `Bearer ${token}`)
        .send({ title: 'Track Actualizado' })
        .expect(200);
      
      expect(res.body.data.title).toBe('Track Actualizado');
    });
  });

  describe('DELETE /api/tracks/:id', () => {
    it('debería eliminar un track', async () => {
      await request(app)
        .delete(`/api/tracks/${trackId}`)
        .set('Authorization', `Bearer ${token}`)
        .expect(200);
    });
  });
});
```

### 2.7 Ejecutar Tests

```bash
# Todos los tests
npm test

# Con watch mode (re-ejecuta al cambiar archivos)
npm run test:watch

# Con reporte de cobertura
npm run test:coverage
```

### 2.8 Configuración Jest (opcional)

```javascript
// jest.config.js
export default {
  testEnvironment: 'node',
  transform: {},
  moduleFileExtensions: ['js'],
  testMatch: ['**/tests/**/*.test.js'],
  collectCoverageFrom: ['src/**/*.js'],
  coveragePathIgnorePatterns: ['/node_modules/', '/tests/'],
  verbose: true
};
```

---

## 3. Monitorización con Slack

> 📂 **Código de ejemplo**: [`codigo/T8/src/utils/handleLogger.js`](../codigo/T8/src/utils/handleLogger.js)

### 3.1 ¿Por qué Slack?

Enviar errores a Slack permite:
- Notificaciones en tiempo real
- Historial de errores centralizado
- Alertas al equipo de desarrollo

### 3.2 Configurar Slack Webhook

1. Ve a [api.slack.com/messaging/webhooks](https://api.slack.com/messaging/webhooks)
2. Crea una nueva app "From Scratch"
3. Activa "Incoming Webhooks"
4. Añade webhook al canal (ej: #logs)
5. Copia la URL del webhook

### 3.3 Instalación

```bash
npm install morgan-body @slack/webhook
```

### 3.4 Crear Logger para Slack

```javascript
// src/utils/handleLogger.js
import { IncomingWebhook } from '@slack/webhook';

const webhook = process.env.SLACK_WEBHOOK 
  ? new IncomingWebhook(process.env.SLACK_WEBHOOK)
  : null;

export const loggerStream = {
  write: (message) => {
    if (webhook) {
      webhook.send({
        text: `🚨 *Error en API*\n\`\`\`${message}\`\`\``
      }).catch(err => console.error('Error enviando a Slack:', err));
    }
    console.error(message);
  }
};

export const sendSlackNotification = async (message) => {
  if (webhook) {
    try {
      await webhook.send({ text: message });
    } catch (err) {
      console.error('Error enviando a Slack:', err);
    }
  }
};
```

### 3.5 Integrar en app.js

```javascript
import morganBody from 'morgan-body';
import { loggerStream } from './utils/handleLogger.js';

// Después de express.json(), antes de las rutas
morganBody(app, {
  noColors: true,
  skip: (req, res) => res.statusCode < 400, // Solo errores
  stream: loggerStream
});
```

### 3.6 Configuración en .env

```env
SLACK_WEBHOOK=https://hooks.slack.com/services/XXXX/YYYY/ZZZZ
```

### 3.7 Notificaciones Personalizadas

```javascript
// En cualquier controlador o middleware
import { sendSlackNotification } from '../utils/handleLogger.js';

export const criticalOperation = async (req, res) => {
  try {
    // Operación crítica...
    await sendSlackNotification('✅ Operación crítica completada');
  } catch (error) {
    await sendSlackNotification(`❌ Error crítico: ${error.message}`);
    throw error;
  }
};
```

---

## 4. Ejercicio del Curso

Para poner en práctica los conceptos de este tema, realiza el siguiente ejercicio:

### API Completa con Documentación y Tests

Extiende tu API del T7 (Blog con Autenticación) añadiendo:

**Parte 1: Documentación Swagger**
- Documenta todas las rutas de auth, posts y users
- Incluye ejemplos de request/response
- Configura autenticación Bearer en Swagger UI

**Parte 2: Tests con Jest**
- Alcanza al menos 80% de cobertura
- Tests para todos los endpoints
- Tests para casos de error y validaciones

**Parte 3: Monitorización**
- Configura alertas a Slack para errores 500
- Añade notificaciones para operaciones críticas

**Ubicación:** `ejercicios/T8/`

```bash
cd ejercicios/T8
npm install
npm test
npm run dev
# Accede a /api-docs para ver la documentación
```

Consulta el README del ejercicio para más detalles.

---

## 5. Resumen

| Herramienta | Propósito | Comando/Endpoint |
|-------------|-----------|------------------|
| **Swagger** | Documentación interactiva | `/api-docs` |
| **Jest** | Framework de testing | `npm test` |
| **Supertest** | Tests de HTTP | `request(app).get(...)` |
| **morgan-body** | Log de peticiones | Middleware |
| **@slack/webhook** | Notificaciones | `webhook.send()` |

---

## 6. Estructura Final del Proyecto

```
T8/
├── src/
│   ├── app.js
│   ├── config/
│   │   └── db.js
│   ├── controllers/
│   │   ├── auth.controller.js
│   │   └── tracks.controller.js
│   ├── docs/
│   │   └── swagger.js
│   ├── middleware/
│   │   ├── error.middleware.js
│   │   ├── session.middleware.js
│   │   └── validate.middleware.js
│   ├── models/
│   │   ├── user.model.js
│   │   └── track.model.js
│   ├── routes/
│   │   ├── auth.routes.js
│   │   └── tracks.routes.js
│   ├── utils/
│   │   ├── handleError.js
│   │   ├── handleJwt.js
│   │   ├── handleLogger.js
│   │   └── handlePassword.js
│   └── validators/
├── tests/
│   ├── auth.test.js
│   └── tracks.test.js
├── .env.example
├── package.json
└── README.md
```

---

## Recursos Adicionales

- [Swagger/OpenAPI](https://swagger.io/specification/) - Especificación OpenAPI 3.0
- [swagger-jsdoc](https://www.npmjs.com/package/swagger-jsdoc) - Genera specs desde JSDoc
- [swagger-ui-express](https://www.npmjs.com/package/swagger-ui-express) - UI para Express
- [Jest](https://jestjs.io/) - Framework de testing
- [Supertest](https://www.npmjs.com/package/supertest) - Testing HTTP
- [Slack Webhooks](https://api.slack.com/messaging/webhooks) - Documentación oficial
- [@slack/webhook](https://www.npmjs.com/package/@slack/webhook) - Cliente Node.js
- [morgan-body](https://www.npmjs.com/package/morgan-body) - Logger de peticiones HTTP

---

## Conclusión del Curso

¡Felicidades! Has completado el curso de **Desarrollo Backend con Node.js y Express**.

A lo largo de los 8 temas has aprendido:

| Tema | Contenido |
|------|-----------|
| **T1** | Fundamentos de Node.js y ESM |
| **T2** | Servidores HTTP nativos |
| **T3** | APIs REST con fetch |
| **T4** | Express y middleware |
| **T5** | MongoDB y Mongoose |
| **T6** | Validación y seguridad |
| **T7** | Autenticación JWT |
| **T8** | Documentación y testing |

### Próximos pasos recomendados

- **Deploy**: Despliega tu API en Railway, Render o Fly.io
- **Docker**: Containeriza tu aplicación
- **CI/CD**: Configura GitHub Actions para tests automáticos
- **WebSockets**: Añade funcionalidad en tiempo real con Socket.io
- **GraphQL**: Explora alternativas a REST
- **TypeScript**: Añade tipado estático a tus proyectos
